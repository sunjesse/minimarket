apiVersion: v1
kind: Service
metadata:
  name: mm-server-lb
  labels:
    app: mm-server
spec:
  type: LoadBalancer
  selector:
    app: mm-server
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
